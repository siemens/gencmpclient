# GitHub Actions for genCMPClient
# SPDX-License-Identifier: Apache-2.0
# Copyright (c) Siemens AG, 2021-2025

name: Windows

on: push
jobs:
  cmake:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: cmake
        run: |
          echo "OPENSSL_DIR=C:\Program Files\OpenSSL" >> $GITHUB_ENV
          echo "GENCMP_NO_TLS=1" >> $env:GITHUB_ENV
          echo "GENCMP_NO_SECUTILS=1" >> $env:GITHUB_ENV
          copy "C:\Program Files\OpenSSL\lib\VC\x64\MD\libcrypto.lib" "C:\Program Files\OpenSSL\lib\"
          copy "C:\Program Files\OpenSSL\lib\VC\x64\MD\libssl.lib" "C:\Program Files\OpenSSL\lib\"
          mkdir build
          cd  build
          cmake ..
          cmake --build . --config Release
          


