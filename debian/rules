#!/usr/bin/make -f

# rules for Debian packaging of libgencmp and cmpclient

%:
	dh $@

override_dh_auto_test:
	@echo "Skipping tests"

override_dh_shlibdeps:
	dh_shlibdeps --dpkg-shlibdeps-params=--ignore-missing-info -l$(CURDIR)/obj-$(DEB_HOST_GNU_TYPE)/libsecutils/src/libsecutils
